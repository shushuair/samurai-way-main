import s from "./ProfileFullName.module.css"

export const ProfileFullName = () => {
    return (
        <div className={s.fullNameContainer}>
            <h2 className={s.fullName}>name</h2>
        </div>
    );
};

